# Experiments Index

One-off or hypothesis-driven corpus experiments live here. These are non-gating research artifacts.

## Active Experiments

- `header-salvage-2026-03-12/`: header normalization experiment to expose deeper parse/runtime behavior past opener noise.

## Expected Experiment Contents

- transform manifest (`manifest.tsv`)
- post-transform eval outputs (`eval-*.json`, `eval-*.md`)
- post-transform gap outputs (`gaps-*.json`, `gaps-*.md`)
- short experiment note (`README.md` in experiment directory)
